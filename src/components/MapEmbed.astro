---
// MapEmbed.astro — Google Maps for Church and Party Location
// Replace the src URLs with the actual Google Maps embed URLs for the real locations
const churchMapSrc =
    "https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3762.661!2d-99.1332!3d19.4326!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x0%3A0x0!2zMTnCsDI1JzU3LjQiTiA5OcKwMDcnNTkuNSJX!5e0!3m2!1ses!2smx!4v1234567890";
const partyMapSrc =
    "https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3763.661!2d-99.1532!3d19.4226!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x0%3A0x0!2zMTnCsDI1JzIxLjQiTiA5OcKwMDgnMzEuMiJX!5e0!3m2!1ses!2smx!4v1234567890";
---

<section class="maps-section" id="ubicacion">
    loading="lazy" referrerpolicy="no-referrer-when-downgrade" allowfullscreen>

    <a
        class="map-card__link"
        href="https://maps.google.com/?q=Direccion+del+Salon"
        target="_blank"
        rel="noopener noreferrer"
    >
        Abrir en Google Maps
        <svg
            width="14"
            height="14"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            ><path
                d="M18 13v6a2 2 0 01-2 2H5a2 2 0 01-2-2V8a2 2 0 012-2h6M15 3h6v6M10 14L21 3"
            ></path>
        </svg>
    </a>
</section>

<style>
    .maps-section {
        background: var(--warm-white);
        overflow: hidden;
    }

    .maps-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        min-height: 600px;
    }

    .map-card {
        height: 600px;
        position: relative;
        background: #f1f3f4;
    }

    .map-card__overlay {
        position: absolute;
        bottom: 2rem;
        left: 2rem;
        right: 2rem;
        pointer-events: none;
    }

    .map-card__info {
        background: white;
        padding: 1.5rem;
        border-radius: 1rem;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        display: inline-block;
        pointer-events: auto;
        animation: fade-in-up 0.8s ease both;
    }

    .map-card__badge {
        display: inline-block;
        font-family: var(--font-sans);
        font-size: 0.6rem;
        letter-spacing: 0.2em;
        text-transform: uppercase;
        color: var(--blue-mid);
        margin-bottom: 0.5rem;
    }

    .map-card__title {
        font-family: var(--font-serif);
        font-size: 1.5rem;
        color: var(--blue-deep);
        margin-bottom: 1rem;
    }

    .map-card__link {
        display: inline-flex;
        align-items: center;
        gap: 0.75rem;
        color: var(--blue-mid);
        text-decoration: none;
        font-family: var(--font-sans);
        font-size: 0.85rem;
        font-weight: 500;
        transition: var(--transition);
        border-bottom: 1px solid transparent;
    }

    .map-card__link:hover {
        color: var(--blue-deep);
        border-color: var(--blue-deep);
    }

    @media (max-width: 900px) {
        .maps-grid {
            grid-template-columns: 1fr;
        }
        .map-card {
            height: 450px;
        }
        .map-card__overlay {
            bottom: 1.5rem;
            left: 1.5rem;
            right: 1.5rem;
        }
        .map-card__info {
            padding: 1.25rem;
        }
    }
</style>
